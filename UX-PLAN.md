# UX改修計画 — 「旅の手帳」を屋外で・片手で・3秒で分かる手帳に

作成 2026-09-12（出発8日前）。現状9画面のスクリーンショット監査と CSS の実測をもとに作成。
デザインの骨格（トラベラーズノート風・紙質感・半券・付箋・朱スタンプ）は**維持**する。変えるのは「読める・触れる・迷わない」の3点。

## 0. 前提と締切

- **凍結日 9/16**。それ以降は危険な誤りの修正のみ（Codex の助言に同意）。したがって Phase 1〜2 を 9/14 までに、Phase 3 は 9/16 までに終わらないものは見送る。
- 変更のたびに 3点セット（sw.js CACHE / V・index.html ?v=）を上げ、375px 幅の描画確認を Read で見せてから push。
- 絵文字アイコン禁止・tel: リンク禁止・DAY5 8:25 hard・検疫YES はそのまま。

## 1. 監査で見つかった弱点

| # | 画面 | 事実（実測） | 困ること |
|---|---|---|---|
| A | 全体 | 行の説明文 `.lrow .d` 11.5px、半券メタ 10px、ラベル 8.5px、シェブロン 8〜10px | 屋外の直射日光・歩きながらでは読めない。iPhone の標準本文は 17px |
| B | 全体 | 補助色 `--ink-faint #6E6250` を小さい文字に多用 | 小さい×薄いの重ね技で可読性が落ちる |
| C | 旅程 | 行の展開はシェブロン（10px）が目印。行全体がタップ対象かが見た目で分からない | 「押せるのか」を毎回試す |
| D | 付箋・半券 | 「MAP」「詳細 ▾」が 8〜9px の小文字 | 最重要の動線（地図を開く）が一番小さい |
| E | 読む | 下タブ「読む」→ サブタブ（雑誌/手引き/情報）→ 章チップ（7つ）の3階層 | どこにいるか分からない。戻り方も3通り |
| F | いま（旅行中） | 上の帯は良いが、その下に当日の全行が並ぶ | 「今から何をするか」だけ見たいのに一日分を読む |
| G | 準備 | ToDo 48件が縦一列。段の見出しと件数はあるが折りたためない | 済んだ段が場所を取り、残りが見えない |
| H | 地図 | キー未設定の説明文が3行常時表示 | 設定済みの端末でも出る文言が邪魔（未設定時のみ表示に） |
| I | 全体 | 下タブが固定で、最後の付箋が隠れる余白ぎりぎり | 一番下の要素が触りにくい |
| J | 全体 | 夜のホテルで見ると紙色が明るすぎる | 寝る前の確認で眩しい |

## 2. 改修方針（設計原則）

1. **本文16px・最小13px**。11.5px 以下の文字を全廃する。ラベル類は大きさで差をつけず、字間と色で差をつける。
2. **コントラスト 4.5:1 以上**（小さい文字は 7:1 目標）。`--ink-faint` は 14px 以上でのみ使う。
3. **タップ対象 44px 以上**。行全体をタップ可能にし、シェブロンは「状態表示」に格下げ。
4. **導線は2階層まで**。下タブ＋ページ内の章。サブタブは廃止して章に吸収する。
5. **「いま」は次の1手だけ**。旅行中は「次の予定」を主役にし、それ以外は畳む。
6. **手帳らしさは要素の"数"でなく"質"で出す**。付箋の角度・テープ・スタンプは1画面に1〜2個まで。

## 3. フェーズ計画

### Phase 1 — 読める・触れる（9/13、半日）— **done 2026-09-12（v28）**

- 型サイズを一段上げる：本文 `.lrow .d` 11.5→14px、見出し `.lrow .h` 14→16px、半券メタ 10→13px、ラベル 8.5→11px、章タイトル 10.5→12px。行間は 1.6 維持。
- `--ink-faint` の使用箇所を棚卸しし、13px 以下では `--ink-soft` に置換。
- 旅程の行：`.lrow` 全体を `role=button` にしてタップで展開。シェブロンは右端に固定し、展開時に回転。行の最小高さ 48px。
- 付箋の「MAP」→「地図で開く」の文字ボタン（13px・44px 高）。半券の「詳細 ▾」→「詳細を見る」。
- 下タブ：高さを 56px＋safe-area に固定し、`body` の padding-bottom を「タブ高＋24px」に。
- 検証：375px 幅で全9画面を撮影。文字サイズの最小値を CSS から grep して 11px 未満がゼロであること。

### Phase 2 — 迷わない（9/14、1日）— **done 2026-09-12（v29）**

- **「読む」の3階層を2階層に**：サブタブ（雑誌/手引き/情報）を廃止し、「読む」ページ冒頭に3つの大きな入口カード（雑誌・手引き・情報）を置く。各ページの上部に「‹ 読むへ」だけを残す。章チップは残すが、現在の章をハイライト（scroll-spy）。
- **「いま」旅行中モード**：帯の下を「次の予定カード（大きく：時刻・場所・徒歩分・地図ボタン）＋ その次の1行」に変え、当日の残り行は「きょうの残りを見る」で展開。過去の行は既定で畳む。
- **準備 ToDo**：段（2〜3ヶ月前／1ヶ月前…）をアコーディオン化し、全件済みの段は自動で畳む。「残りだけ」を既定オンにし、進捗バーは残件数を主表示に。
- **地図**：キー説明はキー未設定時のみ、1行＋「設定する」リンクに縮める。設定済みなら非表示。
- **旅程の日ページ**：日付見出しをスクロール追従（sticky）にし、いま何日目を見ているかを常時表示。前後の日への移動は既存の下部リンクを上部にも置く。
- 検証：主要タスク5つ（次の予定を見る／地図を開く／今夜のホテル住所を出す／もしもの手順／持ち物チェック）がすべて2タップ以内で到達できることを確認。

### Phase 3 — おしゃれに仕上げる（9/15〜16、余力があれば）— **一部 done 2026-09-12（v30：チェックの手応え・キャプション・付箋の傾き）。夜モード・組版統一・トークン化は未着手**

- **見出しの組版**：日本語見出しと英字ラベル（ITINERARY 等）の字間・ベースラインを揃え、英字ラベルは 11px→12px・字間 .18em で統一。
- **写真のキャプション**：全写真で「英語名 — 日本語ひとこと」の形に統一し、キャプション 12px・ink-soft。
- **装飾の間引き**：1画面あたりテープは1本、付箋の傾きは ±1.5° まで。角丸・影の値をトークン化（`--r-card:6px` `--shadow-card`）してばらつきを消す。
- **夜モード（暖色の減光）**：`prefers-color-scheme: dark` で紙色を `#2A2622`、インクを `#E8E0D0` の暖色にする。手帳の質感を保ちつつ眩しさを消す。テクスチャの透明度も調整。
- **チェックの手応え**：ToDo・持ち物のチェック時に朱のチェック印を 150ms でフェードイン。音・振動は付けない。
- **空状態**：雑誌が未着のときの枠を「今朝の号は 6時頃に届きます」の一文＋前号へのリンクに。

### Phase 4 — 検証と凍結（9/16）

- 実機（iPhone 16 Pro）で屋外の直射日光下・片手・歩きながらの3条件を各1分ずつ。読めない文字があれば Phase 1 の数値を上げる。
- Lighthouse のアクセシビリティ項目でコントラスト・タップ対象の警告ゼロ。
- 妻の端末（ホーム画面版）でキー・書類・チェックが引き継がれているかを確認（記憶は端末ごと）。
- 3点セットを上げて push、デプロイ成功を確認し、以後は凍結。

## 4. やらないこと（今回）

- 全面的なデザイン刷新・別案への乗り換え（design-dojo の3案儀式は済んでいる）
- 新機能の追加（BACKLOG は凍結後に扱う）
- 雑誌タブの誌面デザイン変更（自動配信と結合しているため）
- フォントの変更（Google Fonts の初回キャッシュ設計を壊さない）

## 5. 成功の判定

- 11px 未満の文字がゼロ／本文 14px 以上
- 主要5タスクが2タップ以内
- 屋外実機テストで「読めない」箇所ゼロ
- 「いま」を開いて3秒で次の行動が分かる（妻に見せて口頭で答えてもらう）
